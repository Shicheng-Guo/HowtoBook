data.table(clinical, options = list(scrollX = TRUE, keys = TRUE), rownames = FALSE)
```
Warning message:
In data.table(clinical, options = list(scrollX = TRUE, keys = TRUE),  :
  Item 2 is of size 2 but maximum size is 377 (recycled leaving remainder of 1 items)
```
  
